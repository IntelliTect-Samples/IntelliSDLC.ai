# Generated by web-api-discovery. Do NOT commit raw captured HAR files or
# substitution maps that may contain plaintext originals.

# Raw captures (always contain real PII / tokens):
Samples/HAR/
Samples/HAR-Captured/

# Legacy substitution maps (keyed by plaintext originals; never commit):
**/subs.json
**/*.subs.json

# Scrubbed-output workspace artifacts. The .substitutions.json file itself
# only contains SHA-256 hash prefixes (never plaintext PII) and is safe to
# commit if you choose; it is gitignored here by default to keep the noise
# out of diffs.
Samples/HAR-Scrubbed/.substitutions.json
