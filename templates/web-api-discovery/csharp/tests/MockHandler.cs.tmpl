// <auto-generated>
//   Emitted by web-api-discovery (scripts/codegen/tests-emit.js).
//   Reusable Moq-based HttpMessageHandler that returns a canned response body
//   for any outbound request. Wrapped in an HttpClient that the test passes to
//   the generated client's test-only constructor overload.
// </auto-generated>
#nullable enable

using System.Net;
using System.Net.Http;
using System.Text;
using System.Threading;
using System.Threading.Tasks;
using Moq;
using Moq.Protected;

namespace {{Namespace}}.Tests;

/// <summary>
/// Builds an <see cref="HttpClient"/> backed by a Moq mock of
/// <see cref="HttpMessageHandler"/> that always replies with the given JSON
/// payload and HTTP status. Sufficient for unit-testing the typed wrapper
/// methods without making any real network calls.
/// </summary>
internal static class MockHandler
{
    public static HttpClient FromJson(string json, HttpStatusCode status = HttpStatusCode.OK)
    {
        var mock = new Mock<HttpMessageHandler>(MockBehavior.Loose);
        mock.Protected()
            .Setup<Task<HttpResponseMessage>>(
                "SendAsync",
                ItExpr.IsAny<HttpRequestMessage>(),
                ItExpr.IsAny<CancellationToken>())
            .ReturnsAsync(() => new HttpResponseMessage(status)
            {
                Content = new StringContent(json, Encoding.UTF8, "application/json"),
            });
        return new HttpClient(mock.Object);
    }

    public static HttpClient FromFixture(string fixtureFileName, HttpStatusCode status = HttpStatusCode.OK)
        => FromJson(FixtureLoader.ReadText(fixtureFileName), status);
}