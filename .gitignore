# Ephemeral evidence-capture working directory (see .github/skills/evidence-capture/SKILL.md).
.evidence/

# Playwright MCP server output directory (per repo conventions).
.playwright-mcp/

# OS/editor cruft.
.DS_Store
Thumbs.db
*.swp
*~

# Git worktrees managed by Cleanup-Worktree.ps1.
.worktrees/

# Raw HAR captures (see Start-HarRecording). A raw capture runs to hundreds of
# megabytes and carries live session cookies -- only trimmed, scrubbed extracts
# under docs/har-reference/ are ever committed.
#
# `raw.har` is listed literally as well as by suffix: the recorder names the
# file exactly `raw.har`, which `*.raw.har` does NOT match (that pattern needs
# a `.raw.har` suffix). The .har-captures/ rule hides the difference only while
# the capture directory is the default one -- point -CapturesDirectory
# elsewhere and an unignored, credential-bearing capture is one `git add -A`
# away from being committed.
.har-captures/
raw.har
*.raw.har
raw.snapshot.har
raw.snapshot.ndjson
*.snapshot.har
*.snapshot.ndjson

# The operator's HAR profile: real account identifiers and the scrub salt.
.har-profile.json

# Playwright storage state -- a serialized signed-in session.
*.storage-state.json

testResults.xml

# >>> upstream-only >>>
# Dogfood / dry-run artifacts (see scripts/run-dogfood.ps1 and
# docs/dogfood/tripit-dry-run-report.md). The dogfood-output directory is
# the default -Out location when -Out is not passed and TEMP is unsuitable.
# Upstream-only: scripts/run-dogfood.ps1 is not in the upstream-managed
# paths list so
# this entry would be pure noise in consumer .gitignore files.
.dogfood-output/
# <<< upstream-only <<<
